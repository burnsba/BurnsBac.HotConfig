<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <Authors>Ben Burns</Authors>
    <Company>Ben Burns</Company>
    <Description>Code to load and interact with settings from a standardized json file.</Description>
    <PackageLicenseFile>COPYING</PackageLicenseFile>
    <PackageProjectUrl>https://github.com/burnsba/BurnsBac.HotConfig</PackageProjectUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>json, settings</PackageTags>
    <PackageReleaseNotes>initial release</PackageReleaseNotes>
    <FileVersion>1.1.1.1</FileVersion>
    <RepositoryUrl>https://github.com/burnsba/BurnsBac.HotConfig</RepositoryUrl>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|AnyCPU'">
    <DocumentationFile>C:\Users\tolos\code\csharp\BurnsBac.HotConfig\BurnsBac.HotConfig\BurnsBac.HotConfig.xml</DocumentationFile>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" Version="13.0.1" />
  </ItemGroup>

  <ItemGroup>
    <None Include="..\COPYING">
      <Pack>True</Pack>
      <PackagePath></PackagePath>
    </None>
  </ItemGroup>

</Project>
